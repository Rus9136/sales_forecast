# P2 — Улучшение точности модели (по ML_AUDIT_REPORT.md §6)

**Начато:** 2026-07-07
**Предшественники:** `ML_AUDIT_REPORT.md` (аудит), `FIX_PROGRESS.md` (Фазы 0-3 — корректность/измерение закрыты), `docs/FORECAST_IMPROVEMENT_PLAN.md` (этап 4, источник приоритетов).
**Правила:** ценовой движок не трогаем; per-segment не обучаем (закрыто экспериментом −2.3 п.п.); инвариант «строго прошлое» в rolling-фичах сохраняется; каждый трек меряется против базовой линии тем же протоколом.

---

## 0. Рамка: почему P2 — про данные, а не про код

Фазы 0-3 закрыли корректность и измерение. Дальнейшая точность внутренними средствами **исчерпана эмпирически** (журнал плана):

| Ход | Эффект | Вывод |
|---|---|---|
| Свежие данные (retrain) | −0.19 п.п. | шум |
| Сегментные модели | **+2.3 п.п. (хуже)** | откат |
| Optuna-тюнинг (GroupKFold) | 57% CV (хуже 36%) | не применён |
| **Фикс утечки (сделан)** | **−17.9 п.п.** | единственный структурный win, уже в проде |
| Календарь / паритет / post-proc (Фаза 3) | нейтрально | корректностные, не точностные |

**Следствие:** оставшиеся ~10 п.п. точности лежат в новых источниках данных, которых у системы нет. P2 = разметка + внешние данные + horizon-архитектура, а не рефакторинг.

## 📌 Базовая линия (против чего меряем всё ниже)

Прод-модель `trained_at=2026-07-05`, recursive 30-day backtest, 4 окна (из FIX_PROGRESS Фаза 1.6):

| Метрика | Значение |
|---|---|
| Aggregate WAPE | **25.87% ± 2.83** |
| MedianAPE | 20.77% ± 1.42 |
| Monthly WAPE (главная для ФОТ) | 16.73% ± 2.81 |

Худшая зона (из 30-day backtest, план 4.1): **кофейни h=8-14 = 75% MAPE** (резорт-сезонность Tary), Q3 h=8-14 = 60%. Месячный агрегат bias **−13.5%** (занижаем ФОТ-сумму).

Протокол замера: `docker exec ... python /tmp/backtest_30day.py` (rolling-origin, 4 окна, WAPE).

## Состояние данных на 2026-07-07 (проверено в проде)

| Данные | Состояние | Гейт для |
|---|---|---|
| Разметка подразделений | **12 / 91** | Трек 1 |
| `sales_by_waiter` | 2025-04 → 2026-07, 15 мес, 47 точек ✅ | Трек 3 |
| `receipt_item` (SKU) | с 2024-06, ~2 года ✅ | Трек 6 |
| Банкеты / бронирования | интеграции нет ❌ | Трек 4 (блокер) |
| Погода | нет ❌ (open-meteo бесплатен) | Трек 7 |
| Стоп-листы iiko | нет ❌ | Трек 8 |

---

## Треки

Статусы: ⬜ не начат · 🟡 в работе · ✅ закрыт · 🚫 заблокирован (ждёт данных/решения владельца)

### Трек 1 — Разметка 91/91 подразделений ⬜ 🚫(ждёт разметку)

**Аудит/план:** 4.2 (инфраструктура готова с апреля, работает вхолостую при 12/91).
**Что даёт:** резорт-сезонность Tary (Бурабай/Чарын/Колсай) — худшая зона системы. План: кофейни h=8-14 **75% → 50-60%**. Сейчас доля новых фичей в модели ~0.02% (нечего разделять).
**Инженерная часть:** готова (БД миграция 009, UI `/departments`, 18 ML-фичей, зеркалирование train/inference). Код не трогаем.

**❗ Нужно от владельца:** заполнить 79 точек через `/departments` — бренд, location_type (city/mall/business/resort_mountain/resort_lake/visit_center/other), сезонность, часы, месяцы сезона. ~1-2 ч ручного ввода.

- [ ] 1.1 Разметка 79 точек (владелец)
- [ ] 1.2 Retrain + rolling-origin замер против базовой линии
- [ ] 1.3 Зафиксировать эффект на кофейни h=8-14

### Трек 2 — Hybrid horizon-модели ⬜ (можно стартовать без данных)

**Аудит/план:** P1-4, план 4.4 (Вариант B рекомендован).
**Что даёт:** прямое попадание в бизнес-задачу (30-day график смен). `model_short` (h=1-7, богатые lag) + `model_long` (h=8-30, лаги заморожены на t0), диспетчер по `days_ahead`. Чинит протухание lag-фичей и bias месячной суммы −13.5%.
**Измеримость:** готова (`horizon_days`, Фаза 1).

**❗ Нужно от владельца:** только «go» + приоритет. Данных ноль.

- [ ] 2.1 Обучить `model_long` (таргет = продажи через N дней, past-only лаги на t0)
- [ ] 2.2 Диспетчер в `agent.forecast()` по `days_ahead`
- [ ] 2.3 30-day backtest по горизонтам vs базовая линия (цель: h=8-30 не хуже, monthly bias → 0)
- [ ] 2.4 Тест-инвариант + деплой через guardrail Фазы 1.2

### Трек 3 — Фичи смен + волатильность + Fourier ⬜ (данные есть)

**Аудит/план:** 4.5, 4.7, 4.8.
**Что даёт:** `sales_by_waiter` уже 15 мес. Не циклические фичи: `avg_waiters_same_dow_4w`, `unique_waiters_30d`. Плюс дешёвые `cv_30d` (волатильность → резорты) и Fourier-гармоники годового цикла. Эффект ожидается скромный, затраты минимальны.

**❗ Нужно от владельца:** ничего, «go».

- [ ] 3.1 Waiter-фичи в единый feature-builder (train==inference, тест-паритет)
- [ ] 3.2 `cv_30d` + Fourier(doy) гармоники
- [ ] 3.3 Ablation: какие реально в топе gain, мёртвые убрать
- [ ] 3.4 Замер vs базовая линия

### Трек 4 — Банкеты / корпоративы 🚫 (блокер: доступ к API)

**Аудит/план:** 4.3.
**Что даёт:** банкеты **прибавляются** к обороту, объясняют хвост выбросов (Tary Ayusai). Forward-bookings → модель знает «+N к обороту» на дату прогноза. Высокий потолок.

**❗ Нужно от владельца (блокер):** доступ к workforce/бронированиям — **URL API + авторизация**, ИЛИ выгрузка исторических банкетов (дата, точка, сумма/гости). Ключевой вопрос: **есть ли источник и отдаёт ли он будущие брони?**

- [ ] 4.0 Ответ владельца: источник существует? forward-bookings?
- [ ] 4.1 Интеграция + таблица `banquets` (hist + forward)
- [ ] 4.2 Фичи `has_banquet`, `banquet_count`, `banquet_revenue_estimate`
- [ ] 4.3 Retrain + замер на outlier-днях

### Трек 5 — Квантильные прогнозы (P10/P50/P90) ⬜ (ждёт риск-профиль)

**Аудит/план:** P1-5 (модельные квантили, не путать с conformal-CI post-proc — тот сделан), план 4.6.
**Что даёт:** для графика смен интервал полезнее точки. `objective='quantile'` три альфы. alpha≈0.4 → занижение → экономия ФОТ; alpha≈0.55 → запас персонала.

**❗ Нужно от владельца:** риск-профиль ФОТ — **экономить (занижать) или страховаться (завышать)?**

- [ ] 5.0 Ответ владельца: направление bias
- [ ] 5.1 Три квантильные модели (P10/P50/P90)
- [ ] 5.2 Отдавать интервал в `/batch` + labor-demand
- [ ] 5.3 Замер pinball-loss + покрытие интервала

### Трек 6 — SKU intermittent: two-stage ⬜ (данные есть, низкий приоритет)

**Аудит/план:** P0-5/P1-8 (tweedie уже проверен — оставлен log1p, WAPE 70% vs 75%), план №6/12.
**Что даёт:** two-stage (классификатор P(qty>0) × регрессор qty|qty>0) для прерывистого спроса. SKU-модель уже ожила (R² 0.10→0.45, Фаза 2). Дальше — только если labor-demand потребует SKU-точности.

**❗ Нужно от владельца:** решение — нужна ли SKU-точность для TCO сейчас, или dept-модель приоритетнее.

- [ ] 6.1 two-stage прототип, hold-out vs текущий log1p
- [ ] 6.2 Стоп-листы iiko (доступность ≠ спрос) — зависит от Трека 8

### Трек 7 — Погода 🚫 (низкий приоритет, ждёт Трек 1)

**Аудит/план:** план 5.3. open-meteo бесплатен, нужны координаты точек (появятся из разметки Трека 1). Веранды/доставка/резорты.

- [ ] 7.1 Координаты точек (из Трека 1)
- [ ] 7.2 open-meteo backfill + 2 фичи (t°, осадки)
- [ ] 7.3 Замер на резорт-точках

### Трек 8 — Стоп-листы SKU 🚫 (блокер: доступ к iiko API)

**Аудит/план:** план №12. Нулевые продажи из-за отсутствия в меню ≠ нулевой спрос.

**❗ Нужно от владельца:** решение по доступу к iiko stop-list API.

- [ ] 8.0 Доступен ли stop-list API?
- [ ] 8.1 Флаг доступности как фича SKU

---

## Что нужно от владельца — сводка

| # | Вопрос / действие | Блокирует |
|---|---|---|
| 1 | **Разметить 79 точек** через `/departments` (1-2 ч) | Трек 1 (самый выгодный) |
| 2 | Банкеты: есть API/выгрузка? отдаёт будущие брони? | Трек 4 |
| 3 | Риск-профиль ФОТ: занижать (экономия) или завышать (запас)? | Трек 5 |
| 4 | Нужна ли SKU-точность для TCO сейчас? | Трек 6 |
| 5 | Доступ к iiko stop-list API? | Трек 8 |

**Рекомендуемый старт:** Трек 1 (разметка — твоя часть) **параллельно** с Треком 2 (hybrid horizon — моя часть, данных не нужно). Треки 2/3/5 стартуют без данных, только по «go» + риск-профиль.

---

## Журнал

| Дата | Что | Эффект |
|---|---|---|
| 2026-07-07 | Создан план, проверено состояние данных в проде (12/91 размечено, waiter 15 мес, банкетов/погоды/стоп-листов нет) | Базовая линия WAPE 25.87% зафиксирована; треки приоритизированы |
